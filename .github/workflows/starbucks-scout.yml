name: Starbucks Scout

on:
  workflow_dispatch:

jobs:
  scout:
    runs-on: ubuntu-latest

    steps:
      - name: Test Atlas Connection
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        run: |
          echo "================================="
          echo "ATLAS STARBUCKS SCOUT"
          echo "================================="
          echo "Scout is awake."
          echo "Supabase URL loaded:"
          echo $SUPABASE_URL
          echo "Connection test complete."
